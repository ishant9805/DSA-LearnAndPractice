#include<bits/stdc++.h>

using namespace std;

bool helper(int x, int digit) {
    
}

vector<int> countGoodNumbers(int a, int b, int digit) {
	
}

int main() {
    cout << "Starting Program" << endl;
    vector<int> v = countGoodNumbers(20, 45, 1);
    cout << "------Printing Vector------\nVector: {";
    for (int i = 0; i < v.size() - 1; i++)
    {
        cout << v[i] << ", ";
    }
    cout << v[v.size() - 1] << "}" << endl;
    return 0;
}